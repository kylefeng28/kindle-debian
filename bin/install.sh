#!/bin/sh
echo "[*] Creating Mount Point..."
mkdir /mnt/debian
mkdir /mnt/debian/dev
mkdir /mnt/debian/etc
touch /mnt/debian/etc/mtab
mkdir /mnt/debian/proc
mkdir /mnt/debian/sys
mkdir /mnt/debian/mnt
